.DS_Store
# PyCharm project configuration
.idea
__pycache__
# Separation model binaries. Auto-downloaded by audio-separator on first use.
# See README "Credits" for sources. Keep metadata JSONs/YAMLs tracked so we
# don't have to rebuild them on a clean clone.
api/pretrained_models/*.onnx
api/pretrained_models/*.ckpt
api/pretrained_models/*.pth
api/pretrained_models/*.safetensors
songs/
tests/song_files/
node_modules/
api/static-build
api/staticroot/
.env
api/assets/dev/
api/assets/bundles/
.devcontainer
.lsp
test-results/
playwright-report/
google-application-credentials.json
# Intermediate frames for scripts/capture-gifs.mjs
.gif-frames/
